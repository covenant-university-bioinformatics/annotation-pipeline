import { spawn } from 'child_process';
// import { TestJobDto } from "../nats/dto/test.job.dto";
import { BaseSpawn } from './base.spawn';

export class TestSpawn {
  // getJobParameters(testJobDto: TestJobDto): string[] {
  //   return [String(testJobDto.numLines), String(testJobDto.numSeconds)];
  // }
  //
  // spawnAnalysisProcess(jobParameters: string[]) {
  //   this.jobSpawn = spawn("./test.sh", jobParameters, {
  //     signal: this.signal,
  //   });
  // }
}
